# Core dependencies for self-balancing robot with Deep Reinforcement Learning
numpy>=2.0.0
gymnasium>=1.1.0
mujoco>=3.3.0
torch>=2.0.0
scipy>=1.10.0
matplotlib>=3.5.0
PyYAML>=6.0.0
mediapy
ahrs

# Deep Reinforcement Learning
stable_baselines3>=2.0.0
stable-baselines3[extra]
# Experiment tracking and logging
wandb>=0.20.0

# Testing
pytest>=8.0.0

# Optional visualization dependencies (auto-installed with mujoco)
# glfw  # OpenGL context management (comes with mujoco)
# PyOpenGL  # OpenGL bindings (comes with mujoco)